class Country {
  final String name;
  final String code;
  final String confirmed;
  final String recover;
  final String death;

  const Country(this.name, this.code, this.confirmed, this.recover, this.death);
}
